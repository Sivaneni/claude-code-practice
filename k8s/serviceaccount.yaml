apiVersion: v1
kind: ServiceAccount
metadata:
  name: weather-api
  namespace: weather-api
  labels:
    app.kubernetes.io/name: weather-api
# Pods do not need to call the Kubernetes API, so we disable the auto-mounted
# service-account token to reduce the attack surface.
automountServiceAccountToken: false
